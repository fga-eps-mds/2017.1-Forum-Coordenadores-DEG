<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ForumDEG.Views.NewForumPage"
             NavigationPage.HasNavigationBar="True"
             Title="Novo Fórum">
    <StackLayout Padding="20">
        <Entry Text="{Binding _title}" Placeholder="Título"/>
        <DatePicker Date="{Binding _date}" 
                    x:Name="dateDatePicker"/>
        <TimePicker Time="{Binding _hour}" />
        <Entry Text="{Binding _place}" 
               Placeholder="Local"/>
        <Label Text="  Pautas:"/>
        <Editor Text="{Binding _schedules}" 
                HeightRequest="150" />
        <StackLayout Orientation="Horizontal"
                     HorizontalOptions="End">
            <Button Text="Cancelar" 
                TextColor="White"
                BackgroundColor="Orange"/>
            <Button Text="Criar fórum" 
                TextColor="White"
                BackgroundColor="Orange"
                Clicked="OnNewForumButtonClicked"/>
        </StackLayout>
    </StackLayout>
</ContentPage>